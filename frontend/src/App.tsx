import { DayLogPage } from "./DayLogPage";

/**
 * Serves as the top-level app shell for the day-log frontend.
 *
 * @returns The main page component.
 * @example
 * <App />
 */
export function App(): JSX.Element {
  return <DayLogPage />;
}

export default App;
